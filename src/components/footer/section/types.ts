import { FunctionComponent, ReactElement } from "react"

export interface FooterSectionProps {
    children: ReactElement[]
}

export type FooterSectionComponent = FunctionComponent<FooterSectionProps>
