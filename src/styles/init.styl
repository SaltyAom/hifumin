@font-face
    font-family somatic
    src url('/fonts/somatic.otf') format('opentype')
    font-display swap

body
    @apply block font-sans m-0

@media screen and (prefers-color-scheme dark)
    body
        @apply bg-gray-900